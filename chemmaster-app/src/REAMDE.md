This file have main.tsx and App.tsx, cause is designed to run locally, and in a parent app too, so, in te parent App you need to:
1. Install all of the dependences of this project
2. Index the App.tsx to the main router of the app

(Document each folder)